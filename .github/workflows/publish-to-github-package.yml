name: Publish GitHub Packages

on:
  push:
    branches:
      - main
    paths:
      - 'projects/ng-text-highlighter/**'
      - '.github/**'
      - 'scripts/patch-package-json.js'

jobs:
  publish-to-github-packages:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.14.0'
          registry-url: 'https://npm.pkg.github.com'

      - name: Cache npm dependencies
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install dependencies
        run: npm install

      - name: Patch source package.json for GitHub Packages
        run: npm run patch-package

      - name: Authenticate with GitHub Packages
        run: echo "//npm.pkg.github.com/:_authToken=${{ secrets.GITHUB_TOKEN }}" > ~/.npmrc

      - name: Build library and app
        run: |
          npm run build:library
          npm run build:prod

      - name: Print final package.json for verification
        run: |
          echo "Final package.json:"
          cat dist/ng-text-highlighter/package.json

      - name: Publish Library to GitHub Packages
        run: |
          cd dist/ng-text-highlighter
          npm publish

      - name: Create Release Tag (optional)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git tag v$(date +'%Y%m%d%H%M%S')
          git push origin --tags
